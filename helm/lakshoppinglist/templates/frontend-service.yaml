apiVersion: v1
kind: Service
metadata:
  name: lak-frontend-service
  namespace: {{ .Values.namespace }}
spec:
  type: {{ .Values.frontend.service.type }}
  selector:
    app: lak-frontend
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: 80
